@model IEnumerable<CinemaParadiso.Models.Movie>

<div id="moviesList">
    @foreach (var movie in Model)
    {
        <div>
            <h4>
                @movie.Name
            </h4>
            <div>
                @movie.Year
            </div>
            <hr />
        </div>
    }
</div>
